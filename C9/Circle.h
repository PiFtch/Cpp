class Circle {
    public:
        // the radius of this circle
        double radius;

        // Construct a default ciecle object
        Circle();

        // Construct a circle object
        Circle(double);

        // Return the area of this circle
        double getArea();
};